# MQFS — Sentiment-Adjusted Statistical Arbitrage Engine

# Python >= 3.9 required

# 

# Install all dependencies:

# pip install -r requirements.txt

# 

# PyTorch (CPU-only, recommended unless you have a GPU):

# pip install torch –index-url https://download.pytorch.org/whl/cpu

# 

# To use the lightweight VADER fallback instead of FinBERT:

# pip install vaderSentiment

# python run_backtest.py –no-finbert

# –– Core numerics ––

numpy>=1.21.0
pandas>=1.5.0
statsmodels>=0.13.0
scipy>=1.7.0

# –– Market data ––

yfinance>=0.2.28

# –– NLP — FinBERT (primary scorer) ––

transformers>=4.30.0

# torch must be installed separately (see note above)

# –– NLP — VADER (lightweight fallback, no GPU required) ––

vaderSentiment>=3.3.2

# –– Visualisation ––

matplotlib>=3.5.0
